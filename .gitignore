# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables
.env
.env.local
.env.*
!.env.example

# Documentation (exclude all except LICENSE and README)
*.md
!LICENSE.md
!README.md

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# CodeVibes deployment — rendered artifacts carrying per-server values / secrets
deploy/cloud-init.out.yaml
deploy/cloud-init.vars
deploy/cloudflared/config.yml

# SQLite runtime DB + WAL/SHM must never be committed (upstream leaked theirs).
# The real DB lives on the Hetzner volume at runtime, not in the repo.
codevibes-backend/data/*.db
codevibes-backend/data/*.db-wal
codevibes-backend/data/*.db-shm
codevibes-backend/data/*.sqlite*
